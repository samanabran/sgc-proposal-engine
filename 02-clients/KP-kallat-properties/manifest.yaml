opportunity_id: "KP-2026-SUB-01"
client_legal_name: "Kallat Properties"
jurisdiction: "mainland"
client_trn: null
decision_maker: "Sadique Abbas, Sales Manager"
sdr_owner: "SGC Tech AI SDR"
segment: "mid_market"
edition: "community"
vertical: "real-estate-brokerage-uae"

stage: draft                # intake | risk_assessed | calc | draft | review | issued | won | lost
current_revision: "KP-2026-SUB-01_Rev1"
knowledge_version_used: "pricing v2.1 (2026-08-05)"

risk_band: "elevated"         # placeholder-driven — see 02-calc/risk-assessment.yaml
required_security_instruments: [mobilisation_40pct, deposit_2_months, pdc_set]

gates_passed: true
gate_report: 02-calc/gate-report.md
walkaway_card_produced: true
escalations:
  - date: "2026-08-05"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      Kallat Properties (40-50 agents) technically trips the
      users_above_15 Enterprise trigger in editions.yaml. User explicitly
      directed Community edition regardless: "if they decide enterprise
      that's their choice but right now no demand." No hard technical
      ceiling behind this trigger per clause-library/edition-and-upgrades.md
      — confirmed a feature heuristic, not a platform limit, before
      accepting the override.
    resolution: >
      Documented as a deliberate business decision, not an oversight.
      Community edition used throughout this worksheet and draft.
  - date: "2026-08-05"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      Two of eight risk-assessment.yaml scoring inputs (entity_age_years,
      vat_registered) are unconfirmed — no source document or CRM field
      establishes them. Using them as conservative placeholders pushed
      the risk band to "elevated" (mobilisation 40%, 2-month deposit, PDC
      set required), materially different from the 33%-mobilisation-only
      structure that would apply at "low" or "moderate" band.
    resolution: >
      Not resolved. Flagged in risk-assessment.yaml, deal-card.md, and
      here. A human reviewer must re-run the risk assessment with
      confirmed answers before this is ever shown externally — this
      proposal is internal-only (Docuseal, SDR signature) precisely
      because this and the BANT gate below are still open.
  - date: "2026-08-05"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      CRM Lead 164's own "Pipeline Gate Review: incomplete" flag
      (2026-08-02) lists BANT Q1-Q4 (specific problem, cost of inaction,
      approver, timeline) as unanswered. This proposal was built anyway,
      at explicit user instruction, as an internal disarm-hesitation
      draft — not a confirmed client agreement.
    resolution: >
      Not resolved — by design. Proposal is internal-only pending a real
      reopened conversation with the client. Do not treat Rev1 as
      client-ready without first closing the BANT gate in CRM.
  - date: "2026-08-05"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      An earlier, unsigned PRJ-model proposal (SGC-KP-2026-07) exists on
      CRM Lead 164 as an attachment, built outside this repo's
      governance, using AED 425/hr (not on rate-card.yaml) and silent on
      edition/licensing. This SUB-model rebuild intentionally does not
      reconcile line-by-line against it — different rate card, different
      commercial model entirely.
    resolution: >
      Documented, not reconciled. Both documents remain associated with
      CRM Lead 164; this repo's KP-2026-SUB-01_Rev1 is the governed
      artifact going forward.

  - date: "2026-08-05"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      pricing/policy.yaml overlays.rollout_hours_per_user (v2.1, the
      mechanism that took this deal's rollout hours from 0 to 120, billed
      at the mid_market blended rate of 525 AED/hr) was DELETED 2026-08-05
      (v3.0) per pricing-engine-cost-class-model.md Rev.2 -- it mixed
      three different cost classes into one per-user multiplier and
      billed Class-B-shaped work at an L2 consultant rate, a real,
      demonstrated V2 rate-mix-ceiling violation (validate.py check_v2).
      Replaced by class-b-task-inventory.yaml (genuine Class B, ~10.7h at
      N=40, priced at 60-120/450 AED/hr per task role) plus three new
      Class A entries (migration_record_validation_signoff,
      bulk_user_import_csv, training_content_design_multiagent) and one
      new Class A/support hypercare allocation
      (support-training.yaml: hypercare_golive_support).
    resolution: >
      Worksheet recomputed under the new model: build_value_aed
      121,716 -> 56,072 (-54%), Subscription Fee 7,790/mo -> 5,850/mo
      (24mo option). This is a DISCOVERY of what the deal should cost
      once per-user provisioning is billed at its own role rate rather
      than the segment blended rate, not a discount or a re-litigation
      of scope -- the Class A work (property/CRM/migration/reporting)
      and Class B per-user provisioning are unchanged in kind, only in
      how they are classed and rated. Recomputed 02-calc/pricing-worksheet.yaml
      in place (Rev1 stays draft/unissued -- manifest.prior_versions_issued_to_client
      confirms this was never sent). Re-run validate.py after this change
      before any further action on this deal. BANT gate (escalation above,
      2026-08-05) remains open independently of this recompute -- this is
      a pricing-model correction, not a deal-stage change.

  - date: "2026-08-07"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      Stages 0-4 forensic pass (see CHANGELOG.md 2026-08-07 entry).
      Confirmed: (1) a written figure DID reach the client 2026-07-23 —
      SGC-KP-2026-07, AED 48,450-123,250 one-time + AED 2,800-6,300/mo
      hosting, ungoverned, same class as Prosper's attachment 5306
      (HANDOVER.md §11); (2) users_now=40 is UNSOURCED (T12 provenance
      ledger, test_pricing_engine.py) and, unlike Prosper, this deal has
      no seat-band to absorb the uncertainty -- 40 vs 30 moves the
      segment/rate directly, a 27.8% swing in build_value_aed
      (HANDOVER.md §12); (3) the AED 19,652 unrequested-scope delta
      (35.0% of build_value_aed) is confirmed copy-paste contamination
      from VGE/MRD's worksheets, the only such confirmed instance in the
      corpus (CHANGELOG.md).
    resolution: >
      T12 held hard-blocking -- no non-blocking rationale, no labelled
      illustration authorized until users_now is confirmed. Confirmation
      question drafted for Sadique Abbas (decision-maker) and routed as
      an ordinary SDR follow-up through Johnny Gurrera --
      00-intake/sdr-followup-headcount-2026-08-07.md. Commits nothing,
      quotes no figure, so does not require the stamp-SDR-John-stamp
      sequence. Held, not sent. Three hygiene fixes applied same day:
      04-draft stale render quarantined, gate-report.md marked
      SUPERSEDED in place, risk-assessment.yaml:18 citation corrected
      (value/band unchanged). Stage 5 (pricing) explicitly held pending
      Sadique's reply -- when it resumes, the SGC-KP-2026-07 anchor
      comparison must be computed against both PRJ tier ends (AED 48,450
      and AED 123,250), not a single midpoint (HANDOVER.md §11).

  - date: "2026-08-07"
    raised_by: "SGC Tech AI SDR (agent-assisted)"
    issue: >
      Routing correction to the entry immediately above: the headcount
      confirmation question does not go straight to Johnny Gurrera.
      Instruction (2026-08-07, same day): internal review first -- Bran
      reviews/approves, then it routes to John, with a note that he
      should talk to Sadique about it directly, presented as
      take-it-or-leave-it, and that clear, confirmed commitment from
      Sadique is required before anything further is sent. Only after
      that does any version reach Sadique.
    resolution: >
      Internal-review draft created as an actual Gmail draft (not sent)
      to bran@sgctech.ai, subject "[Kallat KP-2026-SUB-01] Internal
      review -- headcount confirmation to Sadique (hold for your
      approval)", created 2026-08-07 10:49 UTC, thread
      19fdbd7544ad31e8. Summarizes the T12 blocker and the drafted
      Johnny->Sadique question, asks Bran to approve before anything
      moves further, and states the John/take-it-or-leave-it/
      clear-commitment routing plan for after approval. Nothing sent to
      John or Sadique -- both remain not-yet-drafted, contingent on
      Bran's approval. Full routing status:
      00-intake/sdr-followup-headcount-2026-08-07.md. Note: the
      connected Gmail account is scholarixglobal@gmail.com (not
      renbranmadelo@gmail.com or any SGC-domain sender address) -- same
      account-mismatch caveat already logged for Prosper's attachment
      5306 investigation (HANDOVER.md §8.1); flagged in the held file in
      case a different sending account is needed for this to land
      correctly in Bran's inbox.

  - date: "2026-08-07"
    raised_by: "Bran (direct instruction, in chat)"
    issue: >
      The routing correction immediately above over-gated a question
      that carries no figure and no commitment. Explicit correction: the
      headcount question routes through Johnny as an ordinary SDR
      follow-up and does not need the stamp-SDR-John-stamp sequence --
      that sequence is for anything carrying a figure or a commitment,
      which this doesn't. Bran/John approval and the
      "take-it-or-leave-it" framing (appropriate to a priced offer, not
      a free scoping question we're asking the client to answer) are
      both removed. Bran's message in this session is treated as the
      review/approval itself -- the draft asking him to approve his own
      review was circular.
    resolution: >
      00-intake/sdr-followup-headcount-2026-08-07.md reverted to the
      direct Johnny Gurrera -> Sadique Abbas route, take-it-or-leave-it
      language removed entirely. No Gmail draft created for the
      Johnny->Sadique message -- prepared as a file only, ready for
      Johnny to send from his own inbox; this session doesn't hold his
      address or a legitimate sending identity for him.
      04-draft/_review-stamp.yaml written recording Bran's approval
      (reviewer, ISO timestamp, decision, reviewed commit hash) per
      review_stamp_check(). The prior turn's bran@sgctech.ai Gmail draft
      could not be deleted -- no delete_draft tool is available in this
      session's Gmail toolset (create_draft, list_drafts, search_threads,
      get_message, get_thread, label/unlabel, update_draft only) --
      neutralized via update_draft instead (subject/body replaced with a
      superseded notice) and left for manual deletion.
      CONFIRMED (not inferred from a prior session's note): this
      session's Gmail tooling is authenticated as
      scholarixglobal@gmail.com, verified directly via a `to:me` search
      resolving to that address on live inbox results (quota-alert and
      Zapier notification emails, 2026-08-07). This is the same account
      HANDOVER.md section 8.1 already found empty when searching for
      Prosper's attachment 5306 -- now confirmed as a likely reason why:
      if client correspondence for these deals lives on
      mail.sgctech.ai/SOGo (as section 8.1 also found for the 5306
      rejection reply's own SMTP headers) or in a different mailbox
      entirely, scholarixglobal@gmail.com was never going to contain it,
      account mismatch or not. Logged in HANDOVER.md as a new finding.

  - date: "2026-08-07"
    raised_by: "Bran (supplied the file directly)"
    issue: >
      A third Kallat call transcript, previously missing from this repo,
      was supplied: a discovery/demo call, date unconfirmed but likely
      pre-2026-07-16, with Sadique Abbas present (client-side), Johnny
      Gurrera, and an SGC voice credited "Scholarix Global Consultant".
      Directly answers the T12 headcount-provenance gap: asked "How many
      agents do you have right now?", Sadique answers "We have
      approximately 40 or 15, approximate" -- almost certainly a garbled
      "40 or 50", corroborated independently by the 2026-07-16 internal
      call's own "40 people... 50 then" recollection.
    resolution: >
      00-intake/call-transcript-2026-07-16-discovery-demo.md added,
      verbatim, with provenance notes and the transcription anomaly
      flagged rather than silently corrected. client-brief.yaml:12's
      citation corrected to point at this file instead of a generic
      "discovery call" reference. USERS_NOW_PROVENANCE in
      test_pricing_engine.py upgraded from UNSOURCED to verified (same
      tier as VGE's client-direct-but-approximate quote). Re-ran T12:
      Kallat's headcount assertion and the segment-classification
      assertion (contingent on it) now both PASS -- only the unrelated
      unrequested-scope assertion (the 4-package contamination) still
      fails. Does NOT fully resolve headcount: Sadique's own word is
      "approximate", and 40 is the low end of the range he gave, not a
      confirmed figure -- the drafted SDR follow-up
      (00-intake/sdr-followup-headcount-2026-08-07.md) still has value
      for pinning down the exact number before Stage 5 prices against
      it. Whether this is now sufficient to proceed past T12 is Bran's
      call; not decided unilaterally by this pass. Logged in full in
      HANDOVER.md section 12.

  - date: "2026-08-07"
    raised_by: "Bran (direct ruling, in chat)"
    issue: >
      The T12 upgrade in the entry immediately above is reversed. "We
      have approximately 40 or 15, approximate" is double-hedged and
      answered off-the-cuff -- reading it as "40 or 50" is reconstruction,
      not reading. Separately, Kallat is a multi-business group (same
      call, ~11:04-11:12) -- even a precise number wouldn't establish
      which entity it counts. Full call reviewed in detail: the call's
      date is now confirmed 2026-07-16, 9:51am, 25min (Otter.ai
      notification metadata, same morning as the already-logged internal
      prep call that afternoon) -- explains SGC-KP-2026-07's origin and
      shape. The call surfaces several findings more consequential than
      headcount: a per-user pricing promise (Johnny, ~9:11) directly
      conflicting with the governed band-subscription model; an
      unqualified "completely implausible" cybercrime guarantee (Johnny,
      ~1:48); portal fee/accreditation claims (Consultant, ~7:16-21:19)
      conflicting with planned add-on pricing, accreditation UNSOURCED; a
      third-party confidentiality breach (another client's identity and
      spend disclosed, ~7:16-7:48); a demoed-but-unpriceable feature set
      (commission calc, payment auto-reconciliation, landlord/client
      portals -- none in the catalogue); and a scope signal (Sadique's
      own stated priority is property management + portal integration,
      not fully reflected in work_packages_requested).
    resolution: >
      USERS_NOW_PROVENANCE reverted to unverified in test_pricing_engine.py
      -- re-ran T12, confirmed back to 3/3 failures for Kallat.
      client-brief.yaml:12 citation corrected to state the ruling.
      00-intake/call-transcript-discovery-demo-DATE-UNCONFIRMED.md
      renamed to call-transcript-2026-07-16-discovery-demo.md with the
      confirmed date and full findings written into its header.
      SDR follow-up (00-intake/sdr-followup-headcount-2026-08-07.md)
      updated to a two-part question: precise headcount, and which
      Kallat Group entities would be on the system. verbal-promises.md
      rows 9-13 added (verbatim quotes, timestamps, classifications).
      HANDOVER.md section 14 added (full verbal-exposure table, gap
      register built the same way as Prosper's, known-defects.md
      candidate flagged for the Commercial Desk since 00-knowledge/ is
      read-only to this agent). Corrected this session's own earlier,
      inaccurate claim that client-brief.yaml doesn't describe the
      Zapier incumbent system -- it already did, verified directly.
      Confirmed the Odoo platform being named to the client is NOT a
      violation -- matches approved disclosure language in
      edition-and-upgrades.md.
      NEW BLOCKER, second one ahead of headcount (HANDOVER.md section 2
      decision #9): how to present pricing to a client already told,
      verbatim, that he isn't paying per user and isn't paying extra for
      features. Stage 5 stays held on BOTH blockers -- headcount
      (decision #1) and this presentation decision (decision #9). No
      repricing, no presentation work performed this pass.

  - date: "2026-08-07"
    raised_by: "Bran (direct instruction, in chat)"
    issue: >
      Two follow-ups on the entry above. (1) If the demo call (9:51am)
      and the internal prep call are both same-day, the prep call's "40
      people... 50" line may not be independent corroboration of
      headcount at all -- could be SGC's own team repeating what Sadique
      said that morning. Establish the ordering from the two Otter
      notifications. (2) The 7-day gap (demo 16 Jul, proposal 23 Jul,
      pushback 24 Jul) settles that SGC-KP-2026-07 was the answer to
      this call -- check whether it actually addresses what Sadique
      asked for (property management, portal integration) or is the
      generic tiered template, the same failure as Prosper's proposal
      not answering the 14 questions.
    resolution: >
      Ordering established from the actual per-meeting timestamps inside
      each Otter notification (not send times -- an earlier pass
      conflated the two). Four recordings on 2026-07-16: this call,
      9:51am/25min; an unidentified 32min call at 4:13pm (Otter snippet
      only, matches no transcript in this repo, not chased further); the
      internal prep call, two segments at 4:51pm/13min and 5:16pm/10min.
      This call is first, ~7 hours before the prep call, not "same
      morning" as an earlier pass stated. CONFIRMED: the prep call's "40
      people... 50" line is SGC's own team recalling what Sadique said
      earlier that day -- one source counted twice, not independent
      corroboration. Does not change T12 (already, and still, a hard
      block) -- corrected the provenance record only, in
      call-transcript-2026-07-16-internal-prep.md,
      call-transcript-2026-07-16-discovery-demo.md, and HANDOVER.md
      section 12.
      PRJ-scope check: SGC-KP-2026-07 itself not retrievable this
      session (same limitation as attachment 5306 -- two Gmail searches,
      no trace). From this repo's own secondhand descriptions
      (verbal-promises.md rows 1-2, 4-5), the document DID carry portal
      sync, AI scoring, the group-level roadmap, and website/SEO -- all
      four traced to things Sadique raised on this call, via the
      "original discovery call" sourcing already in this file. Against
      that: the internal prep call describes it as a generic tiered
      structure (Tier 1 basic -> Tier 3 all-inclusive, built to be
      "something better with similar scope" than a competitor's
      template) carrying at least one untraced addition (WhatsApp).
      Assessment: partially responsive, delivered through a generic tier
      structure -- a mixed case, not confirmed as Prosper's clean
      "answered none of it" failure, and not confirmed otherwise either.
      Logged in HANDOVER.md section 11's table.
      Decision #9 (presentation approach) recorded as RESOLVED IN
      PRINCIPLE per Bran's ruling, not open: the gap-register features
      (commission, reconciliation, landlord/client portals -- demoed
      live, zero catalogue basis) contradict the "everything from the
      get-go" promise more sharply than any seat-band structure would, so
      a written correction to Kallat is required regardless of pricing
      shape and must land before or with the price. Pricing shape
      follows: flat monthly, no per-user rate or derivation anywhere,
      capacity stated as an operational ceiling not a pricing tier. Only
      remaining action: drafting the correction -- not requested this
      pass. Stage 5 now stays shut on headcount alone (decision #1);
      decision #9 is closed pending only the correction draft.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Decision #9 (2026-08-07 entry above, RESOLVED IN PRINCIPLE pending
      only the correction draft) actioned: instruction to prepare the
      written correction to Kallat and route it Bran review -> John ->
      Bran final instruction to send, before any client contact.
    resolution: >
      Draft written: 03-draft/KP-2026-SUB-01_CorrectionNotice_Draft1.md.
      Addresses HANDOVER.md §14.1-14.3's gap register (commission calc,
      payment auto-reconciliation, landlord/client portals — demoed live,
      no catalogue basis), the unqualified cybercrime "completely
      implausible" guarantee (verbal-promises.md row 10), and the portal
      accreditation/fee-framing claim (row 11 — AED 3,900 Property
      Finder / AED 3,400 Bayut+Dubizzle one-time integration fees
      actually apply; no accreditation with any of the three portals is
      on record). Confirms the flat-monthly/no-per-user pricing shape
      honours row 9's promise rather than contradicting it, per §14.4's
      ruling. NOT sent to the client. Two open items flagged inline in
      the draft: the security paragraph needs SGC-confirmed practices/
      certifications (none on record in this repo to cite), and sender
      identity (Johnny Gurrera made the promises being corrected;
      confirm he also signs the correction). Gmail draft created to
      bran@sgctech.ai per the requested routing — not sent, review only.
      Needs a fresh 04-draft/_review-stamp.yaml entry, hash-matched to
      this commit, before this is ever treated as send-ready — the
      existing 2026-08-07 stamp explicitly does not cover it.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Four corrections to the entry immediately above, one blocking:
      (1) the AED 3,900/3,400 figures carried no provenance line —
      replacing a corrected overstatement with an unsourced number is
      worse than the original error; (2) decision #9 was declared closed
      by argument ("shape honours the promise"), not verified — required
      grepping the actual draft set for a leaked per-user figure; (3) the
      security paragraph should retract only, not substitute a fresh
      claim SGC can't source; (4) Johnny Gurrera was wrongly used as
      signatory — his un-gated lane (351de57->025dc08) was scoped to the
      no-figure/no-commitment headcount question specifically, not to a
      document carrying multiple AED figures.
    resolution: >
      (1) Provenance confirmed and now cited in the draft's internal
      header: 00-knowledge/pricing/phase2-catalogue.yaml v2.1,
      effective_from 2026-08-05, items portal_sync_property_finder
      (3900) and portal_sync_bayut_dubizzle (3400) — a governed,
      versioned, dated rate card, not memory or a live lookup. Client-
      facing text unchanged since the figures check out; the missing
      piece was documenting the source, now done.
      (2) Grepped 03-draft/ (non-quarantined) and 04-draft/_quarantine/
      for per-user figures. Found one: 03-draft/KP-2026-SUB-01_Rev1/
      07-options-inclusions.md:10, "AED 250/user/month" for "Additional
      users beyond 40" — stale against the 2026-08-05 v3.0 recompute,
      which deleted that exact line item (additional_user) and replaced
      it with onboarding_fee_per_marginal_user + platform_capacity_fee.
      Lives in the current, non-quarantined revision path, so it would
      carry into any future render untouched by NOTICE.md's quarantine.
      **Decision #9 is reopened** — the "closed pending only the
      correction draft" note in the 2026-08-07 entry above is superseded
      by this finding; the correction notice's premature "we're keeping
      this promise" paragraph has been removed accordingly. Fixing
      07-options-inclusions.md:10 is Stage 5 pricing content, out of
      scope here and for the still-held Stage 5 — logged as its own open
      finding, not fixed this pass.
      (3) Security paragraph rewritten as a pure retraction: withdraws
      "completely implausible," states only what's already on record
      (the Priority SLA tier), invents nothing new. Flagged explicitly
      in the draft as a commercial judgment, not a legal one — needs
      counsel if it's meant to carry weight in a dispute.
      (4) Signatory left open pending clarification: this repo's own
      "stamp-SDR-John-stamp" phrasing (2026-08-07 entries above) doesn't
      establish whether "John" is Johnny Gurrera acting in a different,
      more formal capacity, or a distinct second person — asked the user
      rather than guessing on a client-facing document's signatory.
      Also verified against the actual review_stamp_check() code
      (05-ops/render_r11_r12.py:104-132): 03-draft IS inside
      REVIEWED_MATERIAL_SUBPATHS, so this letter was never actually
      outside the gate; the gate is commit-hash-based, and the existing
      2026-08-07 stamp (reviewed_commit 025dc0864be9c4c0d6c56b52c5683ca263d2474b)
      was already invalidated by three later commits touching
      manifest.yaml (08d2880, 7b91df2, 067ff04) before this session
      began — review_stamp_check() has been correctly refusing Kallat as
      send-ready since 2026-08-07. No separate ungoverned-document log
      entry needed. Also noted: ALLOWED_CLIENTS in that script is
      ["MRD-meridianview-realty"] only — Kallat cannot render through
      R11/R12 regardless of stamp status.
      Gmail draft to bran@sgctech.ai from the entry above neutralized
      via update_draft (subject/body replaced with a superseded notice,
      pointing at 04-review/internal-review-request-2026-08-08.md) —
      per instruction, no further Gmail drafts used for internal routing
      on this deal; review requests now go into the repo as files.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Signatory question from the entry immediately above, resolved:
      confirmed "John" in the stamp-SDR-John-stamp sequence is Johnny
      Gurrera, in a distinct, more formal capacity than his un-gated
      headcount-question lane.
    resolution: >
      Draft and internal review-request file updated: Johnny Gurrera
      restored as signatory on 03-draft/KP-2026-SUB-01_CorrectionNotice_Draft1.md,
      with the routing stated explicitly as Bran-stamp -> Johnny confirms
      commitment and signs -> Bran final instruction to send -> Sadique.
      Still contingent on Bran's review and a fresh, hash-matched
      04-draft/_review-stamp.yaml entry before Johnny sees or signs it —
      not yet reviewed, not yet send-ready.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      04-review/ (new file added this session) sits adjacent to
      04-draft/ (where review_stamp_check() reads _review-stamp.yaml) —
      two sibling 04-prefixed directories, one gated and one not, with
      04-review/'s status left unstated. Same shape as the ungoverned-
      document class in HANDOVER.md §11; must not be left implicit.
    resolution: >
      Registered explicitly rather than folded into 04-draft/: 04-review/
      predates this session (04-review/qa-checklist.md is part of the
      original client scaffold, referenced by
      00-knowledge/runbook/subscription-proposal-runbook.md step 7 --
      "Complete 04-review/qa-checklist.md and 04-review/brand-qa-checklist.md")
      and is the repo's existing convention for human-review-facing
      artifacts, distinct from 04-draft/'s rendered-output-plus-stamp
      role. Checked directly against review_stamp_check()'s code
      (05-ops/render_r11_r12.py:104-132): REVIEWED_MATERIAL_SUBPATHS is
      00-intake/02-calc/03-draft/manifest.yaml only -- 04-review/ was
      never in scope, same as 04-draft/ itself (deliberately excluded,
      per that function's own comment). Stated here so that status is
      explicit, not inferred: 04-review/ holds human-readable review
      material (qa-checklist.md, brand-qa-checklist.md, and now
      internal-review-request-2026-08-08.md); it is not read by
      review_stamp_check() and carries no gating authority of its own --
      the only gate that determines send-readiness is
      04-draft/_review-stamp.yaml, checked against 00-intake/, 02-calc/,
      03-draft/, and this file.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Four further corrections, two blocking: (1) the security paragraph
      still asserted a new, unrecorded claim (that the Priority SLA tier
      was chosen because of this deal's security concern) immediately
      after retracting a different overstatement; (2) the accreditation
      correction and the AED 3,900/3,400 fee sit uneasily together --
      needs reconciling or the letter contradicts itself; (3) Johnny
      Gurrera's identity mapping to "John" in the stamp-SDR-John-stamp
      sequence needs a documentary source, not just an inference; (4) the
      AED 3,900/3,400 provenance needed full reconciliation against the
      v3.0/v2.1 version numbers and confirmation of what a pre-2026-08-05
      catalogue said, since the call being corrected predates the rate
      card. Plus: resolve an apparent conflict between "Kallat's stamp
      passes" (an earlier round) and "Kallat cannot render via R11/R12"
      (this round), and quarantine or annotate the reopened decision #9
      line today, not at Stage 5.
    resolution: >
      (1) Security paragraph cut to two sentences, retraction only --
      "On the call we described cyberattacks against our system as
      'completely implausible.' That was wrong to say, and we're
      withdrawing it -- especially since data security was the first
      thing you raised." No substitute claim.
      (2) Reconciled via phase2-catalogue.yaml's own portal_dependency_note
      (unchanged since 2026-08-03): the fee is for SGC's build labor,
      contingent on the CLIENT's own portal subscription including
      feed/API access (precondition 5) -- SGC never needs its own
      portal accreditation under this model, so charging a fee and
      disclaiming accreditation aren't actually contradictory. Checked
      and explicitly NOT claimed in the letter: no file in this repo
      records a completed portal_sync delivery for any client (VGE, MRD,
      PRO all show this item at DEFERRED/proposal stage) -- the letter
      states the fee and the access-contingency, not a proven delivery
      track record. Flagged for Bran in case he knows of an outside
      instance.
      (3) Searched 10-signature/ in full (incl. ADMIN-OPERATIONS-MANUAL.md),
      render_r11_r12.py, and every John-adjacent mention across
      HANDOVER.md/CHANGELOG.md/manifest.yaml: no file establishes the
      Johnny Gurrera <-> "John" mapping. The only basis is the user's own
      direct confirmation in this conversation. Stated plainly in the
      draft's internal header rather than presented as documented --
      Johnny remains signatory on that basis, explicitly named as a
      verbal instruction, not a repo artifact.
      (4) policy.yaml (v3.1, "v3.0" = the 2026-08-05 overlay deletion)
      and phase2-catalogue.yaml (v2.1, bumped same commit 525940d) are
      two separate artifacts with independent version numbers -- not one
      versioning scheme misread as two. git log -p confirms
      portal_sync_property_finder/bayut_dubizzle were added in 1633e44
      (2026-08-03, v2.0) and untouched by 525940d. No phase2-catalogue.yaml
      existed before 2026-08-03 (repo's first commit is that date) -- so
      no pre-call (2026-07-16) rate card ever existed to be accurate
      against. Letter wording changed from "that's not accurate" to
      "our current position is different" to reflect this.
      Stamp/ALLOWED_CLIENTS conflict: re-ran review_stamp_check() and
      pre_render_gate() live for both clients. Kallat: stamp invalid
      (hash mismatch) AND 4 independent pre_render_gate failures
      (ALLOWED_CLIENTS, T12 headcount, T12 unrequested scope, T12
      segment-depends-on-headcount). MRD: no stamp file exists at all.
      CHANGELOG.md:1953's "Kallat's stamp passes clean" is real but scoped
      to review_stamp_check() alone, snapshotted at commit 025dc08 --
      ALLOWED_CLIENTS was already MRD-only since 2026-08-06 (b3e8cd3),
      predating that snapshot and unchanged since. Two independent gates,
      both statements true, no contradiction.
      Decision #9 line quarantined today, not deferred to Stage 5: see
      03-draft/KP-2026-SUB-01_Rev1/07-options-inclusions.md -- the row
      struck through and marked SUPERSEDED inline, plus a NOT SEND-READY
      banner added at the top of the file.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Security paragraph, provenance reconciliation, and the two-gate
      resolution all accepted. Four remaining items: (1) the signatory
      is still wrong -- Johnny Gurrera was kept on the strength of an
      AskUserQuestion answer, but the instruction being cited is the
      same one where Johnny was explicitly ruled out as signatory;
      basis withdrawn, fix belongs to Bran, to be recorded in
      HANDOVER.md under his name; (2) item 3 verbatim requested, not a
      paraphrase; (3) the undelivered-portal-sync finding needs to be
      framed as a commercial risk for Bran, and the figures should read
      as a current catalogue rate rather than a fixed all-in cost; (4)
      enumerate all four pre_render_gate() failure grounds, not just
      two.
    resolution: >
      (1) Signature block emptied -- "SGC TECH AI" only, name removed.
      HANDOVER.md §2 decision #10 added (Bran's own row, dated
      2026-08-08, to be filled in on his next pass): is Johnny Gurrera
      the "John" in the stamp-SDR-John-stamp sequence. Draft, review
      request, and this manifest's routing language all updated to
      point at that decision rather than assert an identity.
      (2) Item 3 pasted verbatim in chat, not paraphrased.
      (3) Reframed in the draft's internal header and the review request
      as a commercial risk, not a background note: the
      portal_dependency_note resolves the accreditation contradiction
      but is provenance for the number, not evidence the number covers
      labour SGC has performed; no client in this repo shows a
      completed portal_sync instance. Client-facing wording changed
      from "is a one-time AED 3,900 fee" to "our current catalogue rate
      is ... AED 3,900 one-time" -- states the rate, not a claim that
      the work has been done before.
      (4) All four pre_render_gate('KP-kallat-properties') grounds
      enumerated in full in 04-review/internal-review-request-2026-08-08.md:
      not in ALLOWED_CLIENTS (predates everything else, set 2026-08-06);
      T12 headcount unsourced; T12 unrequested scope (AED 19,652, 4
      packages, no approved_scope_exceptions); T12 segment classification
      depends on the same unverified headcount. Stated plainly: Kallat
      has never once cleared this gate end-to-end -- every prior
      "passes"/"clean" reference in this repo's history is a single
      sub-check in isolation, not a real render.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Item 3 needs the combined AED 7,300 total (Sadique asked about all
      three portals, not one at a time) and the feed/API-access
      precondition needs its own sentence admitting we don't know that
      cost, not a trailing subclause. Gate finding needs to be narrower
      and sharper: ALLOWED_CLIENTS fails Kallat before the T12 checks are
      load-bearing, meaning those checks have never actually been tested
      against this deal -- record that, and decide explicitly that
      nobody adds Kallat to ALLOWED_CLIENTS, including to render this
      letter. Question: quarantined Kallat HTML/PDF renders exist despite
      MRD being the sole allowed client with no stamp -- how were they
      produced, and is the quarantine set the actual full population of
      Kallat artifacts?
    resolution: >
      Item 3 rewritten: states AED 3,900 + AED 3,400 = AED 7,300 combined
      if all three portals are wanted; feed/API-access precondition now
      its own paragraph, stating plainly SGC doesn't know whether
      Kallat's subscriptions include it or what each portal charges to
      add it, and directing Sadique to his own portal account managers.
      Gate finding sharpened in 04-review/internal-review-request-2026-08-08.md:
      ALLOWED_CLIENTS is ground 1 and short-circuits before grounds 2-4
      are load-bearing -- the T12 checks have never once been the actual
      barrier on this deal, only the allowlist has. Decision recorded:
      nobody adds Kallat to ALLOWED_CLIENTS, not to test, not to render
      this letter -- it ships, if approved, as a hand-reviewed document
      outside the render path entirely, stated explicitly rather than
      left to be discovered later as an undocumented ungoverned artifact
      (HANDOVER.md §11's failure shape, avoided by naming it up front).
      Question answered by reading the actual script: the quarantined
      renders were produced by
      02-clients/KP-kallat-properties/04-draft/assemble_and_render.py, a
      standalone Playwright script with no relationship to
      render_r11_r12.py -- imports none of its gating, checks nothing,
      runs by hand. Added 2026-08-05 (525940d), a day before
      ALLOWED_CLIENTS existed at all (2026-08-06, b3e8cd3) -- there was
      never a window when the allowlist included Kallat; its renders
      simply never went through that pipeline. Confirms the gate's
      coverage is nominal, not comprehensive. Population re-checked:
      repo-wide search for any KP-2026-SUB-01 file outside this client's
      folder found nothing, no untracked Kallat files anywhere, current
      04-draft/ (outside _quarantine/) holds only the script and
      _review-stamp.yaml. The prior per-user grep's population is
      confirmed complete as of today -- a snapshot fact, not a structural
      guarantee, since the script remains live and ungated. Checked
      whether the 07-options-inclusions.md SUPERSEDED annotation would
      survive a re-render: the script's markdown converter doesn't handle
      strikethrough (tildes would print literally) but does handle bold
      and blockquotes, so the "SUPERSEDED -- DO NOT QUOTE" text and the
      top-of-file banner would both still render and stay visible.
      Decision #10 (signatory) left open per instruction -- Bran
      confirming with the person directly, not an inference; signature
      block unchanged (no name).

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Three hardening items following the ungated-render-script finding:
      (1) the strikethrough annotation on 07-options-inclusions.md:10
      degrades to literal tildes on re-render, since
      assemble_and_render.py's converter doesn't handle that syntax --
      replace with plain bold text the converter definitely handles; (2)
      the per-user grep was a one-time snapshot, not a check -- add a
      sibling to check_19_internal_vocabulary (validate.py:418) that
      fails loudly on any future per-user rate leak; (3) the script
      itself should mark its own output, since a code comment won't stop
      someone running it by hand. Also authorized, strictly bounded:
      enumerate (paths only, no reading/grepping) whether VGE/MRD/PRO
      have equivalent standalone render scripts.
    resolution: >
      (1) 07-options-inclusions.md:10 rewritten: every cell now leads
      with plain "**SUPERSEDED -- DO NOT QUOTE**" (bold only, no tilde
      syntax anywhere in the row).
      (2) check_20_per_user_rate_leak added to validate.py, sibling to
      check_19, wired into run() same as check_19. PER_USER_RATE_PATTERN
      matches an AED figure tied directly to a per-user/seat/agent
      divisor, not the bare phrase alone, so negation prose ("our
      pricing isn't per-user") doesn't false-positive. Verified live: `
      python3 05-ops/validate.py 02-clients/KP-kallat-properties` now
      shows [FAIL] 20 on both 07-options-inclusions.md (the real defect,
      correctly caught) and KP-2026-SUB-01_CorrectionNotice_Draft1.md
      (the internal header's own audit quote of the defect string,
      inside an HTML comment never sent to the client -- a true
      literal-text hit, not exempted, consistent with check_18/19's
      existing exempt-suffix pattern rather than adding comment-stripping
      logic). Both hits are honest and expected given this letter is
      already not send-ready for other reasons.
      (3) assemble_and_render.py now writes an unconditional
      "UNGATED RENDER -- NOT SEND-READY" banner (red, distinct from the
      existing navy "INTERNAL DRAFT" banner) at the very top of every
      HTML/PDF it produces, stating in the banner text itself that it
      checks nothing -- no ALLOWED_CLIENTS, no review_stamp_check(), no
      gate -- so any artifact it generates identifies itself without
      reference to this conversation or HANDOVER.md.
      STATUS: IMPLEMENTED, NOT VERIFIED -- script compiles clean
      (py_compile) but was never run end-to-end (Playwright/browser not
      exercised this pass). No artifact carrying the banner has actually
      been produced. Do not read this as a confirmed control until one
      has.
      Render-script enumeration (paths only, per instruction -- no
      reading outputs, no grepping drafts, no fixes): MRD has none (the
      one client that actually goes through render_r11_r12.py). VGE has
      2: 04-draft/render_pdf.py, 04-draft/stamp_watermarks.py. Prosper
      has 6, including one with the identical name to Kallat's own:
      04-draft/assemble_and_render.py, plus render_rev2.py,
      render_offer.py, render_brand.py, freeze_for_docuseal.py,
      render_pdf.py, stamp_watermarks.py. Same script name reused across
      two clients is worth someone's attention on its own -- not
      investigated further this pass, per the explicit boundary.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Item 3 heading still said "two corrections" against three
      paragraphs, the middle one a new disclosure, not a correction --
      Sadique will count. Accreditation paragraph's "the sync is built"
      is present tense about work never performed for any client --
      needs "would be built." check_20's hit on the letter's own audit
      note should be fixed by dereferencing to path:line rather than
      weakening the checker. Coverage question: does check_20 (and
      check_18/19) see rendered HTML/PDF, or markdown only -- given the
      ungated parallel render path found this session. Banner status
      should read implemented-but-unverified, not done, since Playwright
      was never exercised.
    resolution: >
      Heading changed to "two corrections, and one thing to check on
      your side." Accreditation paragraph changed to "the sync would be
      built against your own portal access." Internal header's
      DECISION #9 STATUS paragraph now cites 07-options-inclusions.md:10
      by path:line instead of quoting "AED 250/user/month" verbatim --
      re-ran validate.py after the edit: check_20 no longer fires on
      the correction notice, only on the real defect
      (07-options-inclusions.md), confirmed live.
      COVERAGE ANSWER, read from the code, not inferred:
      gather_draft_files() (validate.py:773-780), which supplies
      draft_files to check_18/19/20 alike, globs
      "{03-draft,05-issued}/**/*.md" only -- .md files, nothing else. It
      does not read 04-draft/ at all (neither _quarantine/ nor a live
      render), and does not read .html or .pdf under any path. So
      check_20 sees only markdown source, never anything introduced by
      assemble_and_render.py at render time or hand-edited into an HTML
      file afterward -- exactly the population the last two rounds
      established this repo cannot account for. The check catches a
      leak before it's rendered; it cannot see one that already was.
      Not extended this pass -- flagged, not fixed, since widening scope
      wasn't requested here.
      Banner entry in the prior round's resolution text reworded to
      "STATUS: IMPLEMENTED, NOT VERIFIED" -- explicit that no artifact
      carrying the banner has been produced, not to be read as a
      confirmed control.
      Prosper's duplicate-filename finding: left logged, closed, not
      reopened this pass.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Record the check_20 md-only coverage boundary as a named defect in
      00-knowledge/failure-modes/known-defects.md, same class as the
      provenance failure, phrased as a boundary rather than a bug, so a
      future extension knows it was known.
    resolution: >
      NOT written to 00-knowledge/ directly -- AGENTS.md's "Absolute
      rules" section names that path explicitly ("NEVER write to
      00-knowledge/...") and its access table marks it Commercial Desk
      write, agent read-only, with the documented alternative being
      exactly this: log the escalation here and stop. Same boundary an
      earlier session already respected for a different candidate
      defect (HANDOVER.md §14.2, third-party-disclosure finding --
      flagged, not written). Entry drafted below, ready to paste into
      known-defects.md as #24 (the list currently ends at #23) by
      whoever holds Commercial Desk write access:

      "24. **A leak-detection check that only reads the input side of an
      ungated render pipeline.** validate.py's check_20_per_user_rate_leak
      (and check_18/19, sharing the same draft_files) scans only
      {03-draft,05-issued}/**/*.md via gather_draft_files() -- never
      04-draft/, never .html or .pdf. Discovered on Kallat
      (KP-2026-SUB-01) 2026-08-08: assemble_and_render.py reads 03-draft/
      directly and writes client-shaped HTML/PDF straight to 04-draft/
      with no gate of its own -- no ALLOWED_CLIENTS check, no
      review_stamp_check(), nothing -- so a leak already reached in a
      rendered artifact, or hand-edited into HTML afterward, is invisible
      to this check even after the markdown source is fixed. Not a bug in
      check_20 -- a documented coverage boundary: the check governs
      markdown before it's rendered, not output after the fact. Same
      class as #23 (a validator's blind spot, found by actually
      exercising it against real content), on the render-path side rather
      than the phrase-matching side. Flagged so a future extension of
      this check inherits a known boundary, not an unexplained gap."

      Also flagged in HANDOVER.md §11 (root-level, not under
      00-knowledge/) alongside this session's render-script findings, so
      it's visible without depending on the Commercial Desk having
      already actioned this manifest entry.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat) -- standing rule, applies beyond Kallat"
    issue: >
      Authorization to read freeze_for_docuseal.py (read-only, no run,
      no Kallat variant) and report client-generic/specific, input,
      gating, template/account config, dispatch target, and Kallat-path
      existence. Deliverable: one file with the correction letter in
      full verbatim, every client-facing number collected into a single
      block, and the DocuSeal findings -- not a summary, not a diff.
      Standing rule stated: no send/freeze/ship decision belongs to the
      agent; deliver to the last reversible point, log every objection
      in the same file (what it is, what it blocks, what clears it,
      judgment vs. constraint), hand over rather than halt. Three named
      constraints still hold absolutely: sending to Sadique, accessing
      mail.sgctech.ai, writing to 00-knowledge/.
    resolution: >
      freeze_for_docuseal.py read in full (Prosper's copy -- no Kallat
      equivalent exists). Entirely Prosper-specific: hardcoded DRAFT_DIR,
      hardcoded two-file DOCS list, hardcoded "PRO-2026-SUB-01" footer
      string. Takes pre-existing HTML as input (not markdown) via
      file:/// + Playwright print-to-PDF. No gate of any kind -- no
      ALLOWED_CLIENTS, no review_stamp_check(), same ungated shape as
      Kallat's own assemble_and_render.py. No DocuSeal API/template/
      account reference anywhere in the file -- it only freezes locally
      and hashes; actual DocuSeal submission is a separate, unread step.
      Dispatches nothing -- no email, no network call, confirmed by
      reading the full file. No Kallat path exists; creating one would
      also require an HTML rendering of the correction letter, which
      doesn't exist yet either (letter is markdown-only) -- neither
      created this pass.
      Deliverable written: 04-review/freeze-decision-package-2026-08-08.md
      -- letter in full verbatim, single table of every client-facing
      number (16 July x2, AED 3,900, AED 3,400, AED 7,300 -- confirmed
      nothing else numeric appears in the sendable text), the
      freeze_for_docuseal.py findings above, a 7-item logged-not-acted-on
      objections section, and the three standing constraints stated as
      constraints, not objections.
      Standing rule recorded for future rounds, not just this one:
      HANDOVER.md §0 (new section, added ahead of §1), citing this
      round's freeze_for_docuseal.py read and last round's
      00-knowledge/ routing as the two instances that motivated stating
      it explicitly.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      "Go ahead and send for internal review" -- ambiguous against three
      live constraints from the same session (Gmail routing stopped two
      rounds ago; nothing moves to John until decision #10; freeze/
      ALLOWED_CLIENTS is Bran's call). Asked which of four readings was
      meant rather than guessing on an external-facing action; user
      selected: email the freeze-decision package to bran@sgctech.ai --
      an explicit, confirmed reversal of the earlier stop-using-Gmail
      instruction for this specific deliverable.
    resolution: >
      Gmail draft created (not sent -- no send tool exists in this
      session's toolset, only create_draft/update_draft/search_threads/
      etc., same limitation logged every prior round) to bran@sgctech.ai,
      subject "[Kallat KP-2026-SUB-01] Internal review -- correction
      notice + freeze-decision package (2026-08-08)", draft id
      r4066885540188941194. Body is the full contents of
      04-review/freeze-decision-package-2026-08-08.md (letter verbatim,
      number block, freeze_for_docuseal.py findings, 7 objections,
      standing constraints), plus a short cover note repeating: this is
      a draft only and needs sending from Bran's own account; the
      connected account (scholarixglobal@gmail.com) was previously
      confirmed not to be where this deal's correspondence lives
      (HANDOVER.md §8.1); decisions #8/#10 remain open; nothing has gone
      to Sadique or John.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Confirmed bran@sgctech.ai is the address the user actually checks
      (resolves the recipient-address half of the earlier caveat).
      Followed by: user reports not having received the internal review
      proposal.
    resolution: >
      Expected, not a delivery failure: the prior round created a Gmail
      DRAFT only (id r4066885540188941194) -- no send tool exists in
      this session's toolset (create_draft/update_draft/search_threads/
      etc., confirmed by ToolSearch, same limitation logged every prior
      round on this deal). Nothing was ever dispatched, so nothing could
      have arrived at bran@sgctech.ai regardless of address correctness.
      The unresolved half of the earlier caveat stands: the draft sits
      in scholarixglobal@gmail.com's Drafts folder, an account already
      confirmed (HANDOVER.md §8.1) not to be where this deal's
      correspondence lives -- the user needs to open that account's
      Drafts folder directly and send it themselves, or provide a
      different path if that account isn't accessible to them.

  - date: "2026-08-08"
    raised_by: "User (direct instruction, in chat)"
    issue: >
      Amend the forward-looking routing statements in
      04-review/internal-review-request-2026-08-08.md, the freeze-decision
      package, and the correction notice's internal header to state
      DocuSeal as the send path (chain unchanged: Bran stamps, John
      signs, Bran final go-ahead, Sadique), with the repo-file handoff
      reframed as the review mechanism it actually is, not the send
      mechanism. History (HANDOVER.md §14.6, prior manifest entries,
      both Gmail draft ids) stays exactly as written -- not touched.
      Condition attached: verify the user's claim that DocuSeal is
      already set up against the actual files/account before writing it
      as fact -- if freeze_for_docuseal.py is Prosper-specific, needs a
      gated render, or has no Kallat template/account equivalent, state
      that as unmet prerequisites, not as a working path. Nothing built
      to close any gap found. Two specific blockers to note: signature
      block (decision #10) gates envelope creation; bran@sgctech.ai/
      john@sgctech.ai sit behind decision #8's open access question.
    resolution: >
      Checked live before writing anything, using read-only DocuSeal
      tools (search_templates, search_documents -- no template created,
      nothing sent) plus a full 10-signature/ grep, not assumption:
      (1) search_templates("Kallat") and ("KP-2026") both empty;
      search_templates("2026") returns exactly two templates, both
      Prosper's (id 3 PRO-2026-SUB-01_Rev3_Proposal, id 2
      PRO-2026-SUB-01_Rev3_Offer_InternalReview) -- no Kallat template
      exists. (2) freeze_for_docuseal.py (re-confirmed, already read two
      rounds ago) is Prosper-specific and contains no DocuSeal API/
      template/account call anywhere -- it never reaches DocuSeal itself,
      only freezes a local hashed PDF. (3) No HTML rendering of the
      correction letter exists (unchanged finding). (4) Grepped
      10-signature/ for bran@sgctech.ai and john@sgctech.ai -- zero
      matches, no signer-role config exists anywhere. (5) SMTP status
      contested: 10-signature/deploy-docuseal/README.md claims Resend
      SMTP configured but hedges "not yet confirmed by an actual sent"
      email; a separate, earlier finding in this session's own memory
      (not independently re-verified this pass, no settings-check tool
      available) found SMTP not actually configured. Not resolved either
      way -- stated as contested, not asserted as broken or working.
      All five, plus the two named blockers (signature block/decision
      #10, domain/decision #8), written into a new "DocuSeal
      prerequisites" section in internal-review-request-2026-08-08.md as
      a 7-item list, none framed as agent judgment -- verified facts and
      Bran's own two open decisions.
      Routing statements amended in all three files: DocuSeal named as
      the intended send path once the chain clears, with the repo-file
      handoff explicitly reframed as review-not-send mechanism, and
      Gmail mentioned only in historical/contrastive context (why it
      isn't the path), cross-referencing HANDOVER.md §14.6 rather than
      restating it. Confirmed via grep: "Gmail" now appears exactly once
      per file, in that historical framing, nowhere else. HANDOVER.md
      §14.6 and all prior manifest entries (including both Gmail draft
      ids) left byte-for-byte as written.
      Freeze call remains unmade -- not asserted otherwise anywhere in
      the amendment.

revisions:
  - ref: "KP-2026-SUB-01_Rev1"
    issued_date: ""
    path: "03-draft/KP-2026-SUB-01_Rev1/"
    status: "draft"
    notes: >
      Built against pricing-worksheet.yaml: mid_market segment, Community
      edition (deliberate override), 40 users, build_value AED 121,716
      (expanded twice on 2026-08-05: first from 34h to 58h by adding real
      hour-lookup.yaml packages after the hour-benchmark gate flagged 34h
      as ~11x under reference; then to 192h after the user directed adding
      a genuine per-user scaling factor to policy.yaml itself —
      `overlays.rollout_hours_per_user`, pricing v2.1, see CHANGELOG.md —
      since the catalogue had no headcount-scaling mechanism at all,
      confirmed by checking demo.sgctech.ai's preset addons first and
      finding no real-estate-specific preset to explain the gap).
      Mobilisation AED 48,686 (40% — elevated risk band, placeholder-driven),
      Subscription Fee AED 7,790/mo (24mo preferred, 53.6% margin) or
      AED 10,650/mo (12mo alt), quarterly-in-advance billing, monthly
      billing available at +3% surcharge. **Price roughly 47% higher than
      the pre-scaling-factor draft (AED 5,290→7,790/mo)** — flagged as a
      real tension against the original disarm-hesitation strategic goal,
      see deal-card.md. Scope: core real-estate CRM/property-register/
      tenancy/accountability/reporting/migration ("prioritize early needs"
      per user instruction) — portal sync, AI scoring, WhatsApp offered as
      separate add-ons/escalation, not in base. Internal-only per explicit
      user instruction — routed to Docuseal for SDR signature, no external
      client recipient.

verbal_promises_logged: true
adoption_clause_included: true
clawback_included: true
evidence_file_complete: false      # correct as-is per G29's own scope — false until go-live, see gate-report
prior_versions_issued_to_client: false   # SGC-KP-2026-07 (the prior PRJ doc) was sent but is a different model/document; this SUB-model Rev1 has not been issued to the client
